fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy==2.0.35
psycopg2-binary==2.9.10
alembic==1.13.2

# Config
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0
python-dotenv==1.0.1

# Security
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.9

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
asyncssh==2.24.0

# Web Search Tool (general_chat)
# Catatan: package resmi di-rename dari 'duckduckgo-search' -> 'ddgs'
# oleh maintainer aslinya (deedy5) di rilis v8+. Versi lama
# duckduckgo-search==6.3.5 sempat dipakai tapi memaksa impersonate
# profile browser ('chrome_119') yang sudah di-drop curl_cffi terbaru,
# menyebabkan error "Invalid impersonate: chrome_119" saat runtime.
ddgs==9.14.4
trafilatura==1.12.2